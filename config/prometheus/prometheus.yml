global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090'] # Prometheus scrapes itself

  - job_name: 'node_exporter'
    static_configs:
      - targets: ['node-exporter:9100'] # Prometheus scrapes the node-exporter container